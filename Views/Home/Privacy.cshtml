@model string
@{
    ViewData["Title"] = "Gerador de Pet";
}
<h1>@ViewData["Title"]</h1>

<a role="button" class="btn btn-primary" asp-controller="Pet" asp-action="getImage">Gerar</a>
@if (Model != null) { <p>Model</p> }
